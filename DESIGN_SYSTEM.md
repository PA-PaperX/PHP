# 🎨 Design System

คู่มือการออกแบบ (Design System) ของโปรเจค "ไอย๊าห์ Iya" (IT Support & Equipment Management System) เน้นความสะอาดตา มินิมอล (Minimal) แต่แฝงไปด้วยความรู้สึกพรีเมียมและทันสมัย

## 🔴 ธีมสี (Color Palette)
- **Primary (สีหลัก - Coral):** `#FF8383` (ใช้สำหรับปุ่มกดหลัก, สถานะ Active, หรือจุดที่ต้องการเน้นความสนใจ)
- **Background (พื้นหลัง):** `#FFFFFF` (เน้นพื้นขาวสะอาดตา เพื่อให้คอนเทนต์โดดเด่น)
- **Foreground (สีข้อความหลัก):** `#000000` (เพื่อให้เกิด High Contrast ทำให้อ่านง่าย)
- **Neutral (สีรอง):** ใช้สีเทาโทน Slate (Slate Grays) สำหรับเส้นขอบ (Borders), ข้อความรอง, และองค์ประกอบที่ไม่ต้องการให้เด่นเกินไป

## ✍️ ฟอนต์และตัวหนังสือ (Typography)
- **ภาษาไทย (Primary Font):** `Kanit` (ใช้อ่านง่ายและดูทันสมัย)
- **ภาษาอังกฤษ/ตัวเลข (Secondary Font):** `Inter`
- **ข้อกำหนด:** หัวข้อ (Headings) ควรใช้ตัวหนาและคลีน ส่วนข้อความทั่วไป (Body Text) กำหนดขนาดมาตรฐานให้อ่านง่ายที่ `16px`

## 📏 ระยะห่างและขอบมน (Spacing & Border Radius)
- ใช้ระบบ Grid พื้นฐานที่ระยะ `4px` หรือ `8px`
- **`radius-md` (8px):** ใช้เป็นมาตรฐานสำหรับช่องกรอกข้อมูล (Inputs) และปุ่มกดขนาดเล็ก
- **`radius-lg` (12px):** ใช้เป็นมาตรฐานสำหรับกล่องการ์ด (Cards) และหน้าต่าง Pop-up (Modals)

## 🧩 ส่วนประกอบหน้าจอ (Components)
- **Cards (การ์ด):** ให้ใช้ขอบสีเทาอ่อน (`border-gray-200`) และใส่เงาแบบบางๆ (Subtle shadows) ให้ดูมีมิติ
- **Buttons (ปุ่มกด):** เน้นขอบมน และใช้สีสว่าง (Coral) ตัดกับตัวอักษรสีขาว
- **Inputs (ช่องกรอกข้อมูล):** ขอบคลีนเรียบๆ เมื่อกดคลิก (Focus) ให้แสดงเส้นขอบสี Coral

## ✨ แอนิเมชัน (Animation)
- **ห้ามแข็งทื่อ:** ใช้ `@vueuse/motion` เพื่อทำให้เกิด Transition แบบนุ่มนวล
- พื้นหลังสามารถมีลูกเล่น SVG Pattern แบบออร์แกนิกที่ลอยเอื่อยๆ (Floating effect) เพื่อความสวยงาม แต่ห้ามรกจนรบกวนสายตาการใช้งาน
