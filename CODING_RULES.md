# 📜 Coding Rules

เพื่อความเป็นระเบียบและลดปัญหาข้อขัดแย้งในระบบ ทีมผู้พัฒนาและ AI ทุกตัวจะต้องปฏิบัติตามกฎเหล่านี้อย่างเคร่งครัด

## ❌ สิ่งที่ห้ามทำ (Don'ts)
1. **ห้ามใช้ Axios:** ในฝั่ง Frontend ไม่อนุญาตให้ใช้ Axios หรือไลบรารี Fetch ตัวอื่น ให้ใช้ `$fetch` หรือ `useFetch` ที่มากับ Nuxt 3 เป็นหลัก
2. **ห้ามนำ Framework นอกเข้ามา:** ไม่อนุญาตให้ติดตั้งหรือใช้ React, Next.js, Bootstrap, Shadcn-Vue ฯลฯ ให้ใช้เฉพาะสิ่งที่มีอยู่ใน `STACK.md` เท่านั้น (เช่น Nuxt UI)
3. **ห้ามแก้ไขไฟล์ระบบที่เสถียรแล้ว (Do not modify stable code):** ห้ามแตะต้องหรือ Refactor ไฟล์ที่ทำงานได้สมบูรณ์อยู่แล้ว เว้นแต่จะได้รับคำสั่งเจาะจง
4. **ห้ามเดาและตัดสินใจเองในเรื่องสำคัญ:** หากเจอความกำกวมหรือต้องเลือกโครงสร้างสำคัญ ต้องถามผู้ใช้ (User) หรือแอดมินให้ยืนยันก่อนเสมอ
5. **ห้าม Commit ลง `main` ตรงๆ:** การปรับปรุงโค้ด ต้องทำผ่าน Pull Request (PR) ตามกฎของ Branch Protection

## ✅ สิ่งที่ต้องทำ (Dos)
1. **ใช้ Composition API เสมอ:** การเขียน Vue Components ต้องใช้ `<script setup lang="ts">` ทุกครั้ง
2. **ใช้ Native UI Components:** พยายามใช้ UI Components พื้นฐานของ `@nuxt/ui` (เช่น `UButton`, `UCard`) ก่อนที่จะไปเขียน CSS หรือ Tailwind ขึ้นมาใหม่เอง
3. **สำรองไฟล์ (Backup) เสมอ:** ก่อนจะแก้ไขไฟล์หลักของระบบ ให้สร้างไฟล์สำรองนามสกุล `.bak` ทิ้งไว้เสมอ เผื่อต้องย้อนกลับ (เช่น `AppHeader.vue.bak`)
4. **ตรวจสอบความเข้ากันได้ (Conflict Check):** ก่อนเพิ่มโค้ด หรือปลั๊กอินใหม่ ต้องเช็คเสมอว่ามันไปตีกับระบบเดิมหรือตรรกะของฐานข้อมูลเดิมหรือไม่
